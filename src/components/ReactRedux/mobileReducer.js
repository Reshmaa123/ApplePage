import { buy_mobile } from "./mobile";
const initialState={
    noOfMobiles:60
}
cost mobileReducer=(state=initialMobile,action)=>{
    switch(action.type){
        case buy-mobile:
            return{
                noOfMobiles:state.noOfMobiles-1
            }
            default:
                return state:
    }
}export default mobileReducer